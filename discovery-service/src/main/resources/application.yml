spring:
  application:
    name: discovery-service
  security:
    user:
      name: eureka
      password: 123456
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761